---
"better-auth-attio-plugin": patch
---

Add support for better-auth 1.7. Update session, cookie, verification and invitation APIs. Use an atomic, short-lived handoff token for impersonation links, enforce Better Auth impersonation and invitation permissions, require real users and members for attribution, and align responses with the Attio app schemas.
